# Database connection string (PostgreSQL)
DATABASE_URL=postgresql://username:password@localhost:5432/civic_resolv

# Server port
PORT=5000

# Optional: Set your public domain for Expo builds (used for production builds)
# EXPO_PUBLIC_DOMAIN=localhost:5000
